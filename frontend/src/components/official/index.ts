/**
 * The Official Record primitives — GaDong's house design system.
 *
 * Five components cover most screens; everything else composes from them.
 * It is a DOCUMENT, not a dashboard: no cards, no drop shadows, no rounded
 * containers. Buttons and seals use 2px; nothing else is rounded.
 */
export { Field } from './Field';
export { Seal } from './Seal';
export { Button } from './Button';
export { DataTable } from './DataTable';
export { Notice } from './Notice';
