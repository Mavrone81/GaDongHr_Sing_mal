{
  "name": "@hrms/entity-client",
  "version": "1.0.0",
  "description": "Fail-closed resolver mapping a LegalEntity to its country context",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.test.js"
    ]
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
